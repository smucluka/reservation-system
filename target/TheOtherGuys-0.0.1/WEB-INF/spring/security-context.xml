<beans:beans xmlns="http://www.springframework.org/schema/security"
    xmlns:beans="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:context="http://www.springframework.org/schema/context"
    
    xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
                        http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security.xsd
http://www.springframework.org/schema/context
http://www.springframework.org/schema/context/spring-context.xsd">
		

	<!-- Resources access (images, css, jquery) -->
	<http pattern="/resources/**" security="none" />

	<!-- This is where we configure Spring-Security -->
	<http auto-config="true" use-expressions="true" create-session="always">
		<intercept-url pattern="/login" access="isAnonymous()" />
		<intercept-url pattern="/potvrdaRegistracije*" access="isAnonymous()" />
		<intercept-url pattern="/registracija" access="isAnonymous()" />
		<intercept-url pattern="/pocetna" access="permitAll" />
		<intercept-url pattern="/pregled" access="permitAll" />
		<intercept-url pattern="/kontakt" access="permitAll" />
		<intercept-url pattern="/onama" access="permitAll" />
		<intercept-url pattern="/korisnik/profil" access="permitAll" />
		 <access-denied-handler error-page="/pocetna" />

		<form-login 
			login-page="/login" 
			authentication-failure-url="/login-error"
			default-target-url="/pocetna" 
			/>
		
		<logout invalidate-session="true" logout-url="/logout" delete-cookies="JSESSIONID"  logout-success-url="/pocetna" />
		<!-- enable csrf protection -->
		<csrf disabled="true" />

		<!-- Headers -->
<!-- 		<headers> -->
<!-- 			<frame-options policy="SAMEORIGIN" /> -->
<!-- 			<hsts disabled="true" /> -->
<!-- 		</headers> -->
		
		<!-- session -->
		<session-management>
		    <concurrency-control max-sessions="1" session-registry-alias="sessionRegistry"/>
		</session-management>
		
		
<!-- 		<session-management invalid-session-url="/login?invalidsession=true" > -->
<!-- 				<concurrency-control max-sessions="3" error-if-maximum-exceeded="true" expired-url="/login?invalidsession=true"/> -->
<!-- 		</session-management> -->
		
	</http>
	
	<beans:bean id="sessionRegistry" 
		class="org.springframework.security.core.session.SessionRegistryImpl"/>
	
	<!-- Authentication manager -->
	<authentication-manager>
		<authentication-provider  ref="customAuthenticationProvider" />
	</authentication-manager>
	
	<beans:bean id="customAuthenticationProvider" 
	class="hr.fer.opp.projekt.security.CustomAuthenticationProvider" />
	
<!-- 	<global-method-security pre-post-annotations="enabled" /> -->
	
	<context:component-scan base-package="hr.fer.opp.projekt" />
	
</beans:beans>